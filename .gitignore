# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# local database (libSQL/SQLite)
/local.db
/local.db-*
*.sqlite
*.sqlite-*

# env (NEVER commit — holds API keys)
.env
.env.*
!.env.example

# misc
.DS_Store
*.pem
*.tsbuildinfo

# local storage for uploaded/rendered media (object storage stands in here for dev)
/storage

# local editor/preview config (absolute machine paths)
/.claude
